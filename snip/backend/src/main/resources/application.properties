# Server
#server.port=8080

# PostgreSQL DataSource
#spring.datasource.url=jdbc:postgresql://localhost:5432/snip_db
#spring.datasource.username=postgres
#spring.datasource.password=123
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.properties.hibernate.format_sql=true

# Base URL used to build short links
#app.base-url=http://localhost:3000

# Rate limiting config
#app.rate-limit.max-requests=5
#app.rate-limit.window-seconds=60




###################


# Server
server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/snip_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# App config
app.base-url=${BASE_URL:https://smart-url-shortener-1-ofq2.onrender.com}
app.rate-limit.max-requests=5
app.rate-limit.window-seconds=60
